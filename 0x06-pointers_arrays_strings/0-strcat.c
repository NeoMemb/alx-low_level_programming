#include "main.h"
#include <string.h>
/**
 * _strcat - concatenate str
 * @dest: desination of cat str
 * @src: source of to-be-cat str
 *
 * Return: catenated string
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
