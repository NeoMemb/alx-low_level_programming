#include "main.h"
#include <string.h>
/**
 * _strlen - print the length of the char
 * @s: the char type
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
