#include "main.h"
/**
 * _abs - printing absolute value
 * @n: The data type int to be checked
 *
 * Return: n (If n is greater than 0)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
